import { NextRequest,NextResponse } from "next/server"; import { isV2Error,requestID,v2Context } from "@/lib/v2/auth"
export async function GET(request:NextRequest){const c=await v2Context(request);if(isV2Error(c))return c;const limit=Math.min(Math.max(Number(request.nextUrl.searchParams.get("limit")??20),1),20);const {data,error}=await c.db.rpc("suggested_friends_v2",{p_viewer:c.user.id,p_limit:limit});return error?NextResponse.json({error:"Unable to load suggestions",requestId:requestID(request)},{status:500}):NextResponse.json({suggestions:data},{headers:{"Cache-Control":"private, no-store","X-Request-ID":requestID(request)}})}
